/*
El sitio de inicio (plantilla) de webMatrix tiene las siguientes tecnologías:

- Node
- Oauth
- Jade
- Jquery Mobile
- LessCSS (Es una librería Javascript que amplia comportamientos dinámicos CSS)
- Express (Framework)

La carpeta views tiene la interfaz de usuario.
Está hecho en jade y normalmente se usa con Express

La lógica que sigue para nuestro enrutamiento, es encuentra en la carpeta routes
Aquí está el archivo home.js que tiene toda la lógica de vistas que hay en nuestro sitio

En la carpeta public se sirven todos esos ficheros con un manejador de archivos estáticos.
Usa lesscss para renderizar el .less y hacerlo .css

Ver www.socket.io
*/